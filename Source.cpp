void genPrim(char* argv, PrimeList& numList)
{
	int i = 0;
	int j =
	unsigned long int upperBnd = checkComLine(argc, argv);
	for (i = 3; i <= upperBnd; i + 2)
	{
		while (i %  != 0)
		{

		}
	}
}
void removeNonTwin(PrimeList& numList)
{
	if ((node2 - node1 != 2) && (node3 - node2 != 2))
	{
		remove(num);
	}
}
